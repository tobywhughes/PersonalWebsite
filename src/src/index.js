import React from 'react';
import ReactDOM from 'react-dom';
import Header from './header';
import './index.css'

const App = 
(   <div>
        <Header />
        <p>cout &lt;&lt; "A Quick Introduction" &lt;&lt; endl;</p>
    </div>
);


ReactDOM.render(App, document.getElementById('root'));
